name: Deploy

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v2
        with:
          node-version: 16

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: 📂 Remove existing index.html
        run: |
          rm -f ./lunch.wolfskii.dev/public_html/index.html

      - name: Copy bot.js and remove .d.ts files
        run: |
          cp ./build/discord/bot.js ./lunch.wolfskii.dev/public_html/discord/bot.js
          rm -f ./build/discord/bot.d.ts ./build/discord/commands/commandList.d.ts ./build/discord/commands/menu.d.ts ./build/discord/interfaces/Command.d.ts ./build/discord/utils/menu.d.ts ./build/utils/menuScraper.d.ts

      - name: 📂 FTP deployment
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ./build/
          server-dir: ./lunch.wolfskii.dev/public_html/
